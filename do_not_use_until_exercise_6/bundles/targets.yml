targets:

  dev:
    mode: development
    workspace:
      host: https://dbc-639f4875-165d.cloud.databricks.com
      # We explicitly specify /Workspace/Users/anders.rorvik@knowit.no to make sure we only have a single copy.
      root_path: /Workspace/Users/anders.rorvik@knowit.no/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: anders.rorvik@knowit.no
        level: CAN_MANAGE
    run_as:
      user_name: anders.rorvik@knowit.no

  prod:
    mode: production
    workspace:
      host: https://dbc-639f4875-165d.cloud.databricks.com
      # We explicitly specify /Workspace/Users/anders.rorvik@knowit.no to make sure we only have a single copy.
      root_path: /Workspace/Users/anders.rorvik@knowit.no/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: anders.rorvik@knowit.no
        level: CAN_MANAGE
    run_as:
      user_name: anders.rorvik@knowit.no


